# ParkAtClient

### Init:
npm install

npm install -g @ionic/cli native-run cordova-res

### Development:
npm start

### Init Deploy:
npm install -g firebase-tools

### Deploy:
npm run build-prod

npm run deploy